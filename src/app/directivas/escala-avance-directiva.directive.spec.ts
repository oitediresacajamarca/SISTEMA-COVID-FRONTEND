import { EscalaAvanceDirectivaDirective } from './escala-avance-directiva.directive';

describe('EscalaAvanceDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new EscalaAvanceDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
